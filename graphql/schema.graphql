type Query {
  # getNewsletterModalConfig: NewsletterModalConfig
  getNewsletterList(
    interval: CollectionIntervalInput
    sort: CollectionSortInput
    search: String
    filters: [CollectionFilterInput]
  ): CollectionCustomers @cacheControl(scope: PRIVATE)
}

type Mutation {
  unsubscribe(ids: [String]): UnsubscribeResponse!
}
